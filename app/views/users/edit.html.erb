<% provide :title, t(".provide_title") %>
<% provide :button_text, t(".btn_submit") %>

<h1><%=t ".title" %></h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render "form" %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <%= link_to t(".change"), Settings.users.edit.email, target: "_blank" %>
    </div>
  </div>
</div>
